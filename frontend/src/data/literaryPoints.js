/**
 * GeoJSON FeatureCollection — starts empty; locations are added dynamically
 * via Book Search ("Map It") or PDF Upload.
 */
export const literaryGeoJSON = {
  type: "FeatureCollection",
  features: [],
};
